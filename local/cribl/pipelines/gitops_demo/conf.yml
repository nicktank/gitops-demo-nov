output: default
groups: {}
asyncFuncTimeout: 1000
functions:
  - id: eval
    filter: "true"
    disabled: null
    conf:
      add:
        - name: index
          value: "'main'"
        - name: env
          value: "`splunk-${C.env.CRIBL_ENV}`"
description: adding things via GitOps
